# 技術設計書

**プロジェクト名**: ChatGPT Bulk Deleter  
**バージョン**: v1.0  
**作成日**: 2026-02-04

---

## 📋 目次

1. [要件定義](#要件定義)
2. [システムアキテクチャ](#システムアーキテクチャ)
3. [API仕様](#api仕様)
4. [データ構造](#データ構造)
5. [ファイル構成](#ファイル構成)
6. [UI設計](#ui設計)
7. [実装計画](#実装計画)
8. [エラー処理](#エラー処理)
9. [テスト計画](#テスト計画)

---

## 要件定義

### 機能要件

#### 1. 削除対象
- **チャット履歴**: 全ての会話履歴
- **メモリ**: パーソナライズ設定の全てのメモリ

#### 2. 削除方法
- **一括選択削除**: 全選択ボタンで全件選択して一括削除
- **個別選択削除**: チェックボックスで個別に選択して削除

#### 3. 優先順位（v1.0）
- **最優先**: チャット履歴の一括削除 + メモリの個別選択削除
- **後回し**: チャット履歴の個別選択削除 + メモリの一括削除

### 非機能要件

#### 1. UI/UX
- **起動方法**: ツールバーアイコンをクリック
- **表示形式**: タブで切り替え（チャット履歴 / メモリ）
- **削除前確認**: 確認ダイアログを表示
- **削除後動作**: 削除されたアイテムを一覧から消し、続けて操作可能

#### 2. データ表示
- **チャット履歴**: タイトル + 日付
- **メモリ**: 省略表示（最初の50文字） + ホバーで全文表示
- **表示件数**: スクロールで追加読み込み（初回28件）

#### 3. パフォーマンス
- **削除中**: 進行状況を表示（例: 25/100件削除中）
- **エラー処理**: 失敗したアイテムはスキップして次へ、最後にまとめて表示

#### 4. スコープ外（v1.0では実装しない）
- キーボードショートカット
- 検索・フィルタ機能

#### 5. 配布方法
- GitHubでzipファイル配布
- Chrome Web Storeには公開しない

---

## システムアーキテクチャ

### 全体構成

```
┌─────────────────────────────────────┐
│  Chrome Extension                   │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────────┐                   │
│  │   Popup UI   │ ← ユーザーが操作  │
│  └──────┬───────┘                   │
│         │                           │
│         ↓                           │
│  ┌──────────────┐                   │
│  │  API Client  │ ← ChatGPT APIを呼び出し
│  └──────┬───────┘                   │
│         │                           │
└─────────┼───────────────────────────┘
          │
          ↓
┌─────────────────────────────────────┐
│  ChatGPT Backend API                │
├─────────────────────────────────────┤
│  /backend-api/conversations         │
│  /backend-api/conversation/{id}     │
│  /backend-api/memories              │
│  /backend-api/memory/{id} (推測)    │
└─────────────────────────────────────┘
```

### コンポーネント構成

```
src/
├── popup/
│   ├── popup.html       # ポップアップのHTML
│   ├── popup.ts         # ポップアップのメインロジック
│   ├── popup.css        # ポップアップ専用のスタイル
│   ├── api-client.ts    # API呼び出しロジック
│   ├── ui-components.ts # UI部品（タブ、リスト、ダイアログなど）
│   └── types.ts         # 型定義
```

---

## API仕様

### 1. チャット履歴取得API

```
GET https://chatgpt.com/backend-api/conversations
    ?offset={offset}&limit={limit}
```

**パラメータ**:
- `offset`: 開始位置（デフォルト: 0）
- `limit`: 取得件数（デフォルト: 28）

**レスポンス**:
```json
{
  "items": [
    {
      "id": "698350d1-ada8-8399-ac8e-8a5abc456898",
      "title": "テスト確認",
      "create_time": 1234567890.123,
      "update_time": 1234567890.123
    }
  ],
  "total": 150,
  "limit": 28,
  "offset": 0
}
```

### 2. チャット履歴削除API（推測）

```
DELETE https://chatgpt.com/backend-api/conversation/{id}
```

**パラメータ**:
- `id`: 会話ID

**レスポンス**:
```json
{
  "success": true
}
```

### 3. メモリ取得API

```
GET https://chatgpt.com/backend-api/memories
    ?exclusive_to_gizmo=false&include_memory_entries=true
```

**レスポンス**:
```json
{
  "memories": [
    {
      "id": "6ad1886b-8895-4ca3-af7b-98cbd63ada02",
      "content": "ユーザーは...",
      "updated_at": "2026-01-15",
      "status": "warm"
    }
  ],
  "memory_max_tokens": 10000,
  "memory_num_tokens": 8211
}
```

**特徴**:
- ページネーションなし（全件取得）
- 総件数は `memories.length` で判断

### 4. メモリ削除API（推測）

```
DELETE https://chatgpt.com/backend-api/memory/{id}
```

**または**

```
POST https://chatgpt.com/ces/v1/m
Body: { "id": "{id}", "action": "delete" }
```

**レスポンス**:
```json
{
  "success": true
}
```

**注意**: 実装後に動作確認して、必要に応じて調整

---

## データ構造

### TypeScript型定義

```typescript
// チャット履歴
interface Conversation {
  id: string;
  title: string;
  create_time?: number;
  update_time?: number;
}

interface ConversationsResponse {
  items: Conversation[];
  total: number;
  limit: number;
  offset: number;
}

// メモリ
interface Memory {
  id: string;
  content: string;
  updated_at: string;
  gizmo_id: string | null;
  status: string;
  conversation_id: string | null;
  created_timestamp: string | null;
  last_updated: string | null;
  labels: string[] | null;
}

interface MemoriesResponse {
  memories: Memory[];
  memory_max_tokens: number;
  memory_num_tokens: number;
}

// UI用の状態
interface AppState {
  activeTab: 'chat' | 'memory';
  conversations: Conversation[];
  memories: Memory[];
  selectedConversations: Set<string>;
  selectedMemories: Set<string>;
  isLoading: boolean;
  isDeleting: boolean;
  deleteProgress: {
    current: number;
    total: number;
  };
  errors: DeleteError[];
}

interface DeleteError {
  id: string;
  title?: string;
  content?: string;
  error: string;
}
```

---

## ファイル構成

### 新規作成するファイル

```
src/popup/
├── popup.html              # ポップアップのHTML
├── popup.ts                # メインロジック
├── popup.css               # スタイル（新規作成）
├── api-client.ts           # API呼び出し
├── ui-components.ts        # UI部品
└── types.ts                # 型定義

public/
└── popup.html              # ビルド後のHTML

dist/
└── popup/
    ├── popup.html
    ├── popup.js
    └── popup.css
```

### 既存ファイルの扱い

| ファイル | 扱い |
|---------|------|
| `src/content/*` | **削除または非使用**（DOM操作ベースのため不要） |
| `src/content/styles.css` | **非使用**（ポップアップ用に新規作成） |
| `manifest.json` | **更新**（popup設定を追加） |

---

## UI設計

### ポップアップの構造

```
┌─────────────────────────────────────┐
│  ChatGPT Bulk Deleter        [×]    │
├─────────────────────────────────────┤
│  [チャット履歴] [メモリ]     ← タブ  │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │ チャット履歴タブ             │   │
│  ├─────────────────────────────┤   │
│  │ ☐ 新しいチャット  2024/01/15 │   │
│  │ ☐ Python修正     2024/01/14 │   │
│  │ ☐ レシピ相談     2024/01/13 │   │
│  │ ☐ ...                        │   │
│  │                             │   │
│  │ [スクロールで追加読み込み]   │   │
│  └─────────────────────────────┘   │
│                                     │
│  [全選択] [全解除] 選択: 0件        │
│                        [削除]       │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  メモリタブ                          │
├─────────────────────────────────────┤
│  ☐ ユーザーは最も自然な日本語で...  │
│     [マウスホバーで全文表示]         │
│  ☐ 森本さんの「減量ログ5」を...    │
│  ☐ 森本さんの「カリステニクス...   │
│                                     │
│  [全選択] [全解除] 選択: 0件        │
│                        [削除]       │
└─────────────────────────────────────┘
```

### ポップアップのサイズ

```css
width: 500px;
height: 600px;
```

### カラースキーム

```css
/* ダークモード対応（ChatGPTに合わせる） */
--bg-primary: #212121;
--bg-secondary: #2f2f2f;
--text-primary: #ececec;
--text-secondary: #b4b4b4;
--accent: #10a37f;  /* ChatGPTの緑 */
--danger: #ef4444;  /* 削除ボタン */
--border: #4d4d4d;
```

---

## 実装計画（3時間版）

### 🎯 スピード重視の実装戦略

**基本方針**:
- MVP（最小限の機能）に集中
- UI/UXの細かい調整は後回し
- エラーハンドリングは必要最小限
- まず動くものを作る → 改善は後で

---

### フェーズ1: 基礎構築（45分）

#### ステップ1.1: ポップアップの骨組み（20分）
- [ ] `popup.html` の作成（シンプルな構造）
- [ ] `popup.css` の作成（最小限のスタイル）
- [ ] `popup.ts` の基本構造
- [ ] タブ切り替え（シンプルな実装）

**成果物**: 開くポップアップ（データなし）

#### ステップ1.2: API Client + データ表示（25分）
- [ ] `api-client.ts` でチャット履歴取得のみ
- [ ] リストに表示（シンプルなループ）
- [ ] ローディング表示（簡易版）

**成果物**: チャット履歴が表示される

**省略**: メモリ取得（後で実装）、エラーハンドリング（最小限）

---

### フェーズ2: チャット履歴の削除（60分）

#### ステップ2.1: 選択機能（15分）
- [ ] 全選択ボタンのみ（個別選択は後回し）
- [ ] 選択件数の表示

**成果物**: 全選択できる

#### ステップ2.2: 削除実装（45分）
- [ ] 削除APIの呼び出し（ループで順次削除）
- [ ] 簡易的な確認ダイアログ（`confirm()`）
- [ ] 進行状況の表示（シンプルなカウンター）
- [ ] 削除後のリスト更新

**成果物**: チャット履歴を一括削除できる

**省略**: 個別選択、詳細なエラーログ、アニメーション

---

### フェーズ3: メモリの削除（60分）

#### ステップ3.1: メモリの表示 + 選択（20分）
- [ ] タブ切り替えでメモリを取得
- [ ] メモリリストの表示
- [ ] 全選択ボタン

**成果物**: メモリが表示され、全選択できる

#### ステップ3.2: メモリの削除（40分）
- [ ] 削除APIの呼び出し（推測版）
- [ ] エラー時のフォールバック（別のエンドポイントを試す）
- [ ] 削除後のリスト更新

**成果物**: メモリを削除できる

**省略**: ホバー表示、詳細なエラーハンドリング

---

### フェーズ4: 仕上げ（15分）

#### ステップ4.1: 最終調整
- [ ] UIの微調整（見た目を整える）
- [ ] manifest.json の更新
- [ ] 動作確認

**成果物**: 完成した拡張機能

**省略**: スクロール読み込み（初回28件のみ）、詳細なエラーログ

---

### 総実装時間の見積もり（3時間版）

| フェーズ | 内容 | 時間 |
|---------|------|------|
| フェーズ1 | 基礎構築 | 45分 |
| フェーズ2 | チャット削除 | 60分 |
| フェーズ3 | メモリ削除 | 60分 |
| フェーズ4 | 仕上げ | 15分 |
| **合計** | - | **3時間** |

---

### 🔻 v1.0で省略する機能

以下は後のバージョンで実装：

- ✂️ **チャット履歴の個別選択**（全選択のみ）
- ✂️ **スクロール読み込み**（初回28件のみ表示）
- ✂️ **メモリのホバー表示**（切り詰め表示のみ）
- ✂️ **詳細なエラーログ**（簡易的なアラート）
- ✂️ **進捗バー**（シンプルなテキスト表示）
- ✂️ **リトライ機能**（エラーはスキップ）
- ✂️ **アニメーション**（なし）

---

### 📋 実装の優先順位

#### 最優先（v1.0に必須）
1. ✅ チャット履歴の全選択 + 一括削除
2. ✅ メモリの全選択 + 一括削除
3. ✅ 削除前の確認
4. ✅ 削除後のリスト更新

#### 後回し（v1.1以降）
- ⏭ 個別選択
- ⏭ スクロール読み込み
- ⏭ 詳細なエラーログ
- ⏭ UI/UXの改善

---

## エラー処理

### エラーの種類

#### 1. ネットワークエラー
- **原因**: インターネット接続の問題
- **処理**: スキップして次へ、最後にまとめて表示

#### 2. 認証エラー
- **原因**: セッションの有効期限切れ
- **処理**: エラーダイアログを表示、ユーザーに再ログインを促す

#### 3. APIエラー
- **原因**: API仕様の変更、レート制限など
- **処理**: エラーメッセージを記録、スキップして次へ

#### 4. 不明なエラー
- **原因**: 予期しないエラー
- **処理**: エラーログに記録、スキップして次へ

### エラー表示UI

```
削除完了: 95件
失敗: 5件

以下のアイテムの削除に失敗しました：
• チャット1 (ネットワークエラー)
• チャット2 (認証エラー)
• メモリ3 (APIエラー: 404 Not Found)
...

[閉じる] [エラーログをコピー]
```

---

## テスト計画

### 単体テスト

| 機能 | テストケース |
|------|------------|
| **API Client** | • 正常系: データ取得成功<br>• 異常系: ネットワークエラー<br>• 異常系: 認証エラー |
| **削除機能** | • 正常系: 1件削除<br>• 正常系: 複数件削除<br>• 異常系: 削除失敗 |
| **スクロール** | • 正常系: 追加読み込み<br>• 異常系: 最後のページ |

### 統合テスト

| シナリオ | 確認内容 |
|---------|---------|
| **チャット履歴の一括削除** | 1. 全選択<br>2. 削除<br>3. 進行状況表示<br>4. 完了メッセージ |
| **メモリの個別削除** | 1. 個別選択<br>2. 削除<br>3. 完了メッセージ |
| **エラー処理** | 1. ネットワークを切断<br>2. 削除実行<br>3. エラーログ表示 |

### 手動テスト

- [ ] ポップアップの表示
- [ ] タブ切り替え
- [ ] データ取得
- [ ] スクロール読み込み
- [ ] チェックボックス選択
- [ ] 削除実行
- [ ] エラー処理
- [ ] パフォーマンス（大量削除時）

---

## 技術スタック

### 開発環境
- **言語**: TypeScript
- **ビルドツール**: Vite（既存）
- **スタイル**: 純粋なCSS（フレームワークなし）
- **UI**: Vanilla JavaScript（フレームワークなし）

### Chrome拡張機能API
- **Manifest V3**
- **Permissions**: 
  - `https://chatgpt.com/*`
  - `storage`（設定保存用）
- **Action**: `default_popup`

---

## セキュリティ考慮事項

### 1. 認証
- **方法**: Cookieベースの認証（ChatGPTのセッション）
- **注意**: ユーザーがログインしている必要がある

### 2. CORS
- **問題**: ポップアップからのAPI呼び出しはCORSに引っかかる可能性
- **解決**: `host_permissions` を `manifest.json` に追加

### 3. データ保存
- **方針**: 削除対象のIDなど、機密情報はローカルに保存しない
- **例外**: ユーザー設定（削除前確認の有無など）は `chrome.storage.local` に保存

---

## パフォーマンス最適化

### 1. データ取得
- **初回**: 28件のみ取得（APIのデフォルト）
- **スクロール**: 28件ずつ追加読み込み
- **メモリ**: 全件取得（ページネーションなし）

### 2. 削除処理
- **方式**: 順次削除（並列削除はレート制限のリスクあり）
- **ディレイ**: 各削除の間に200-500msのランダムディレイ
- **プログレス**: UIは100ms毎に更新

### 3. UI更新
- **方式**: 仮想スクロール（メモリが多い場合）
- **レンダリング**: 可視領域のみレンダリング

---

## manifest.json の更新

```json
{
  "manifest_version": 3,
  "name": "ChatGPT Bulk Deleter",
  "version": "1.0.0",
  "description": "ChatGPTのチャット履歴とメモリを一括削除",
  
  "action": {
    "default_popup": "popup/popup.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },
  
  "permissions": [
    "storage"
  ],
  
  "host_permissions": [
    "https://chatgpt.com/*"
  ]
}
```

**変更点**:
- `content_scripts` を削除（不要）
- `action.default_popup` を追加
- `host_permissions` を追加（API呼び出し用）

---

## 実装の詳細仕様

### 1. ポップアップのライフサイクル

```typescript
// popup.ts
class PopupApp {
  async init() {
    // 1. UIの初期化
    this.initUI();
    
    // 2. 初期データの取得
    await this.loadInitialData();
    
    // 3. イベントリスナーの設定
    this.attachEventListeners();
  }
  
  async loadInitialData() {
    // アクティブなタブに応じてデータを取得
    if (this.activeTab === 'chat') {
      await this.loadConversations(0, 28);
    } else {
      await this.loadMemories();
    }
  }
}
```

### 2. スクロール読み込み

```typescript
class InfiniteScroll {
  private offset = 0;
  private limit = 28;
  private hasMore = true;
  
  async loadMore() {
    if (!this.hasMore || this.isLoading) return;
    
    this.offset += this.limit;
    const response = await apiClient.getConversations(this.offset, this.limit);
    
    if (response.items.length < this.limit) {
      this.hasMore = false;
    }
    
    this.appendItems(response.items);
  }
  
  onScroll(event: Event) {
    const element = event.target as HTMLElement;
    const bottom = element.scrollHeight - element.scrollTop === element.clientHeight;
    
    if (bottom) {
      this.loadMore();
    }
  }
}
```

### 3. 削除処理

```typescript
class DeleteManager {
  async deleteConversations(ids: string[]) {
    const errors: DeleteError[] = [];
    let successCount = 0;
    
    for (let i = 0; i < ids.length; i++) {
      try {
        // 進行状況を更新
        this.updateProgress(i + 1, ids.length);
        
        // 削除実行
        await apiClient.deleteConversation(ids[i]);
        successCount++;
        
        // ランダムディレイ（レート制限回避）
        if (i < ids.length - 1) {
          await this.randomDelay(200, 500);
        }
        
      } catch (error) {
        // エラーをスキップして次へ
        errors.push({
          id: ids[i],
          error: error.message
        });
      }
    }
    
    // 結果を表示
    this.showResult(successCount, errors);
  }
  
  private randomDelay(min: number, max: number): Promise<void> {
    const delay = Math.random() * (max - min) + min;
    return new Promise(resolve => setTimeout(resolve, delay));
  }
}
```

### 4. 確認ダイアログ

```typescript
async function showConfirmDialog(
  title: string,
  count: number
): Promise<boolean> {
  return new Promise((resolve) => {
    // ダイアログを表示
    const dialog = document.createElement('div');
    dialog.className = 'confirm-dialog';
    dialog.innerHTML = `
      <div class="dialog-content">
        <h3>${title}</h3>
        <p>${count}件のアイテムを削除します。</p>
        <p>この操作は取り消せません。</p>
        <div class="dialog-buttons">
          <button class="btn-cancel">キャンセル</button>
          <button class="btn-confirm">削除</button>
        </div>
      </div>
    `;
    
    dialog.querySelector('.btn-cancel').addEventListener('click', () => {
      dialog.remove();
      resolve(false);
    });
    
    dialog.querySelector('.btn-confirm').addEventListener('click', () => {
      dialog.remove();
      resolve(true);
    });
    
    document.body.appendChild(dialog);
  });
}
```

---

## APIクライアントの実装仕様

### api-client.ts

```typescript
class ChatGPTApiClient {
  private baseUrl = 'https://chatgpt.com/backend-api';
  
  /**
   * チャット履歴を取得
   */
  async getConversations(
    offset: number = 0,
    limit: number = 28
  ): Promise<ConversationsResponse> {
    const response = await fetch(
      `${this.baseUrl}/conversations?offset=${offset}&limit=${limit}`,
      {
        method: 'GET',
        credentials: 'include',  // Cookieを含める
        headers: this.getHeaders()
      }
    );
    
    if (!response.ok) {
      throw new Error(`Failed to fetch conversations: ${response.status}`);
    }
    
    return response.json();
  }
  
  /**
   * メモリを取得
   */
  async getMemories(): Promise<MemoriesResponse> {
    const response = await fetch(
      `${this.baseUrl}/memories?exclusive_to_gizmo=false&include_memory_entries=true`,
      {
        method: 'GET',
        credentials: 'include',
        headers: this.getHeaders()
      }
    );
    
    if (!response.ok) {
      throw new Error(`Failed to fetch memories: ${response.status}`);
    }
    
    return response.json();
  }
  
  /**
   * チャット履歴を削除（推測）
   */
  async deleteConversation(id: string): Promise<void> {
    const response = await fetch(
      `${this.baseUrl}/conversation/${id}`,
      {
        method: 'DELETE',
        credentials: 'include',
        headers: this.getHeaders()
      }
    );
    
    if (!response.ok) {
      throw new Error(`Failed to delete conversation: ${response.status}`);
    }
  }
  
  /**
   * メモリを削除（推測 - 実装後に調整）
   */
  async deleteMemory(id: string): Promise<void> {
    // 方法1: DELETE
    let response = await fetch(
      `${this.baseUrl}/memory/${id}`,
      {
        method: 'DELETE',
        credentials: 'include',
        headers: this.getHeaders()
      }
    );
    
    // 方法1が失敗した場合、方法2を試す
    if (!response.ok) {
      // 方法2: POST
      response = await fetch(
        'https://chatgpt.com/ces/v1/m',
        {
          method: 'POST',
          credentials: 'include',
          headers: this.getHeaders(),
          body: JSON.stringify({ id, action: 'delete' })
        }
      );
    }
    
    if (!response.ok) {
      throw new Error(`Failed to delete memory: ${response.status}`);
    }
  }
  
  /**
   * リクエストヘッダーを取得
   */
  private getHeaders(): HeadersInit {
    return {
      'Content-Type': 'application/json',
      // 必要に応じて追加のヘッダー
    };
  }
}

export const apiClient = new ChatGPTApiClient();
```

---

## UI Components の実装仕様

### ui-components.ts

```typescript
/**
 * タブコンポーネント
 */
export class TabComponent {
  private activeTab: 'chat' | 'memory' = 'chat';
  private onTabChange: (tab: 'chat' | 'memory') => void;
  
  constructor(onTabChange: (tab: 'chat' | 'memory') => void) {
    this.onTabChange = onTabChange;
  }
  
  render(): string {
    return `
      <div class="tabs">
        <button class="tab ${this.activeTab === 'chat' ? 'active' : ''}" data-tab="chat">
          チャット履歴
        </button>
        <button class="tab ${this.activeTab === 'memory' ? 'active' : ''}" data-tab="memory">
          メモリ
        </button>
      </div>
    `;
  }
  
  attachEvents(container: HTMLElement) {
    container.querySelectorAll('.tab').forEach(tab => {
      tab.addEventListener('click', (e) => {
        const target = e.target as HTMLElement;
        const tabName = target.dataset.tab as 'chat' | 'memory';
        this.switchTab(tabName);
      });
    });
  }
  
  switchTab(tab: 'chat' | 'memory') {
    this.activeTab = tab;
    this.onTabChange(tab);
  }
}

/**
 * リストアイテムコンポーネント
 */
export class ListItemComponent {
  renderConversation(conv: Conversation, isSelected: boolean): string {
    const date = conv.update_time 
      ? new Date(conv.update_time * 1000).toLocaleDateString('ja-JP')
      : '';
    
    return `
      <div class="list-item" data-id="${conv.id}">
        <input type="checkbox" 
               class="item-checkbox" 
               ${isSelected ? 'checked' : ''}
               data-id="${conv.id}">
        <div class="item-content">
          <div class="item-title">${this.escapeHtml(conv.title)}</div>
          <div class="item-date">${date}</div>
        </div>
      </div>
    `;
  }
  
  renderMemory(memory: Memory, isSelected: boolean): string {
    const shortContent = this.truncate(memory.content, 50);
    
    return `
      <div class="list-item memory-item" 
           data-id="${memory.id}"
           title="${this.escapeHtml(memory.content)}">
        <input type="checkbox" 
               class="item-checkbox" 
               ${isSelected ? 'checked' : ''}
               data-id="${memory.id}">
        <div class="item-content">
          <div class="item-title">${this.escapeHtml(shortContent)}</div>
          <div class="item-date">${memory.updated_at}</div>
        </div>
      </div>
    `;
  }
  
  private truncate(text: string, maxLength: number): string {
    if (text.length <= maxLength) return text;
    return text.substring(0, maxLength) + '...';
  }
  
  private escapeHtml(text: string): string {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
  }
}

/**
 * 進行状況コンポーネント
 */
export class ProgressComponent {
  render(current: number, total: number): string {
    const percentage = Math.round((current / total) * 100);
    
    return `
      <div class="progress-container">
        <div class="progress-bar">
          <div class="progress-fill" style="width: ${percentage}%"></div>
        </div>
        <div class="progress-text">${current} / ${total}件削除中...</div>
      </div>
    `;
  }
}
```

---

## CSS設計仕様

### popup.css（概要）

```css
/* ==================== 基本レイアウト ==================== */
:root {
  --bg-primary: #212121;
  --bg-secondary: #2f2f2f;
  --bg-hover: #3a3a3a;
  --text-primary: #ececec;
  --text-secondary: #b4b4b4;
  --accent: #10a37f;
  --danger: #ef4444;
  --border: #4d4d4d;
}

body {
  width: 500px;
  height: 600px;
  margin: 0;
  padding: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: var(--bg-primary);
  color: var(--text-primary);
}

/* ==================== タブ ==================== */
.tabs {
  display: flex;
  border-bottom: 1px solid var(--border);
}

.tab {
  flex: 1;
  padding: 12px;
  background: transparent;
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  transition: all 0.2s;
}

.tab.active {
  color: var(--accent);
  border-bottom: 2px solid var(--accent);
}

/* ==================== リスト ==================== */
.list-container {
  height: 450px;
  overflow-y: auto;
  padding: 8px;
}

.list-item {
  display: flex;
  align-items: center;
  padding: 12px;
  margin-bottom: 8px;
  background: var(--bg-secondary);
  border-radius: 8px;
  cursor: pointer;
  transition: background 0.2s;
}

.list-item:hover {
  background: var(--bg-hover);
}

.item-checkbox {
  width: 18px;
  height: 18px;
  margin-right: 12px;
  cursor: pointer;
  accent-color: var(--accent);
}

.item-content {
  flex: 1;
}

.item-title {
  font-size: 14px;
  color: var(--text-primary);
  margin-bottom: 4px;
}

.item-date {
  font-size: 12px;
  color: var(--text-secondary);
}

/* ==================== アクションバー ==================== */
.action-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px;
  border-top: 1px solid var(--border);
  background: var(--bg-secondary);
}

.action-bar-left {
  display: flex;
  gap: 8px;
  align-items: center;
}

.btn {
  padding: 8px 16px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  transition: all 0.2s;
}

.btn-primary {
  background: var(--accent);
  color: white;
}

.btn-danger {
  background: var(--danger);
  color: white;
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* ==================== プログレス ==================== */
.progress-container {
  margin: 12px;
}

.progress-bar {
  width: 100%;
  height: 8px;
  background: var(--bg-secondary);
  border-radius: 4px;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: var(--accent);
  transition: width 0.3s;
}

.progress-text {
  margin-top: 8px;
  font-size: 12px;
  color: var(--text-secondary);
  text-align: center;
}

/* ==================== ダイアログ ==================== */
.dialog-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.dialog-content {
  background: var(--bg-secondary);
  padding: 24px;
  border-radius: 12px;
  max-width: 400px;
}

.dialog-buttons {
  display: flex;
  gap: 12px;
  margin-top: 20px;
  justify-content: flex-end;
}
```

---

## 開発の進め方

### 段階的実装

```
フェーズ1: 基礎構築
    ↓ 動作確認
フェーズ2: チャット削除
    ↓ 動作確認
フェーズ3: メモリ削除
    ↓ 動作確認
フェーズ4: スクロール
    ↓ 動作確認
フェーズ5: 仕上げ
    ↓ 最終テスト
リリース
```

### 各フェーズの完了条件

**フェーズ1**:
- [ ] ポップアップが開く
- [ ] タブを切り替えられる
- [ ] データが表示される

**フェーズ2**:
- [ ] チャット履歴を選択できる
- [ ] 削除が実行できる
- [ ] 進行状況が表示される
- [ ] 削除後にリストが更新される

**フェーズ3**:
- [ ] メモリを選択できる
- [ ] 削除が実行できる
- [ ] エラーログが表示される

**フェーズ4**:
- [ ] スクロールで追加読み込みができる
- [ ] 最後まで読み込める

**フェーズ5**:
- [ ] メモリのホバー表示
- [ ] 全体的なUI改善
- [ ] バグ修正

---

## リスクと対策

### リスク1: 削除APIの仕様が不明

**対策**:
- 推測で実装
- 動作しない場合、エラーメッセージから調整
- フォールバック: DOM操作（現在の方式）

### リスク2: レート制限

**対策**:
- 削除の間にランダムディレイ（200-500ms）
- 並列削除は避ける
- エラー時はリトライせずスキップ

### リスク3: CORS問題

**対策**:
- `host_permissions` を `manifest.json` に追加
- `credentials: 'include'` でCookieを送信

### リスク4: メモリの表示が長すぎる

**対策**:
- 50文字で切り詰め
- ホバーで全文表示
- 必要に応じて仮想スクロール

---

## 完成イメージ

### ポップアップを開いた時

```
┌─────────────────────────────────────┐
│  ChatGPT Bulk Deleter        [×]    │
├─────────────────────────────────────┤
│  [チャット履歴] [メモリ]            │
├─────────────────────────────────────┤
│  読み込み中...                      │
│  ⌛                                 │
└─────────────────────────────────────┘
```

### データ読み込み後

```
┌─────────────────────────────────────┐
│  ChatGPT Bulk Deleter        [×]    │
├─────────────────────────────────────┤
│  [チャット履歴] [メモリ]            │
├─────────────────────────────────────┤
│  ☐ 新しいチャット  2024/01/15       │
│  ☐ Python修正     2024/01/14       │
│  ☐ レシピ相談     2024/01/13       │
│  ...                                │
├─────────────────────────────────────┤
│  [全選択] [全解除] 選択: 0件        │
│                        [削除]       │
└─────────────────────────────────────┘
```

### 削除中

```
┌─────────────────────────────────────┐
│  ChatGPT Bulk Deleter        [×]    │
├─────────────────────────────────────┤
│  [チャット履歴] [メモリ]            │
├─────────────────────────────────────┤
│  ☑ 新しいチャット  2024/01/15 ✓    │
│  ☑ Python修正     2024/01/14 ⌛    │
│  ☑ レシピ相談     2024/01/13       │
│                                     │
│  ▓▓▓▓▓▓▓▓▓▓░░░░░░░░  25/100       │
│  25 / 100件削除中...                │
├─────────────────────────────────────┤
│  [中断]                             │
└─────────────────────────────────────┘
```

### 削除完了

```
┌─────────────────────────────────────┐
│  ChatGPT Bulk Deleter        [×]    │
├─────────────────────────────────────┤
│  削除完了: 95件                     │
│  失敗: 5件                          │
│                                     │
│  以下のアイテムの削除に失敗しました： │
│  • チャット1 (ネットワークエラー)   │
│  • チャット2 (認証エラー)           │
│  ...                                │
│                                     │
│  [閉じる] [エラーログをコピー]      │
└─────────────────────────────────────┘
```

---

## 次のステップ

この技術設計書を確認・承認いただければ、実装を開始します。

### 確認してほしいポイント

1. **アーキテクチャ**: ポップアップベースの構成でOK？
2. **API仕様**: 推測で実装してOK？（後で調整）
3. **UI設計**: デザインのイメージは合っている？
4. **実装計画**: 10-16時間の見積もりは妥当？
5. **ファイル構成**: 新規作成する構成でOK？

質問や変更点があれば教えてください！
